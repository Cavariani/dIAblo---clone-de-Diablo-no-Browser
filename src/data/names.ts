// STUB — to be filled by the monsters content agent. Keep the export names.
/** Name parts for rare elites (pt-BR). Rare elite name = PREFIX + SUFFIX (+ optional TITLE). */
export const ELITE_NAME_PREFIXES: string[] = [];
export const ELITE_NAME_SUFFIXES: string[] = [];
export const ELITE_TITLES: string[] = [];
